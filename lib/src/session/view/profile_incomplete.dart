import 'package:flutter/material.dart';

///
class ProfileIncompleteState extends StatelessWidget {
  ///
  const ProfileIncompleteState({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
